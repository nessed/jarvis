# L1 — Supabase jobs migration and lifecycle proof

## Ownership

You may edit only `db/` and `tests/db/`. Do not commit. Do not edit
`docs/context.md`; report the exact outcome to the orchestrator, who owns the
handoff update. Do not reveal credential values.

## Objective

Apply `db/migrations/0001_jobs.sql` to the configured live Supabase project,
then run `tests/db/test_jobs_integration.py`. The current failure is `PGRST205`
because `public.jobs` is absent. The migration must exist before Meta acceptance
because the webhook only enqueues jobs.

## Safety and verification

- Read configuration only to establish variable presence; never output values.
- Applying this additive migration is authorized. Do not drop or replace tables.
- If SQL editor UI is required, navigate there and stop before any final
  dashboard action for the user. Otherwise use the normal configured migration
  path.
- Report whether the migration applied, the lifecycle-test result, and any
  schema or permission error. Add focused tests only under `tests/db/`.
